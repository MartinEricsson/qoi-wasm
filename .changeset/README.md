# Changesets

This repository uses [Changesets](https://github.com/changesets/changesets) to manage versioning and releases.

- Run `npm run changeset` after making user-facing changes to record release notes.
- Commit the generated files in `.changeset/` along with your changes.
